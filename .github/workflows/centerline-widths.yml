name: Build centerline widths

on:
  workflow_dispatch:
    inputs:
      config_path:
        description: Path to centerline config
        required: true
        default: config/centerline-widths.json
        type: string
      centerline_only:
        description: Keine Analyse, nur Centerline ausgeben
        required: true
        default: false
        type: boolean
      upload_pmtiles:
        description: Upload PMTiles to Easyname FTP
        required: true
        default: true
        type: boolean

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 180

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gdal-bin tippecanoe lftp

      - name: Install Python dependencies
        run: pip install -r requirements-centerline.txt

      - name: Build centerline widths
        run: python centerline_widths/run_centerline_widths.py --config "${{ inputs.config_path }}"

      - name: Transform output to WGS84
        run: |
          mkdir -p work/centerline-widths dist/centerline-widths
          if [ "${{ inputs.centerline_only }}" = "true" ]; then
            INPUT_FILE="work/centerline-widths/centerlines_metric.geojsonseq"
            OUTPUT_FILE="work/centerline-widths/centerlines_4326.geojsonseq"
          else
            INPUT_FILE="work/centerline-widths/centerline_widths_metric.geojsonseq"
            OUTPUT_FILE="work/centerline-widths/centerline_widths_4326.geojsonseq"
          fi

          if [ -s "$INPUT_FILE" ]; then
            ogr2ogr -f GeoJSONSeq \
              -s_srs EPSG:31256 \
              -t_srs EPSG:4326 \
              "$OUTPUT_FILE" \
              "$INPUT_FILE"
          else
            echo "No input generated; skipping transform: $INPUT_FILE"
          fi

      - name: Build PMTiles
        run: |
          if [ "${{ inputs.centerline_only }}" = "true" ]; then
            INPUT_FILE="work/centerline-widths/centerlines_4326.geojsonseq"
            LAYER_NAME="centerlines"
          else
            INPUT_FILE="work/centerline-widths/centerline_widths_4326.geojsonseq"
            LAYER_NAME="centerline_widths"
          fi

          if [ -s "$INPUT_FILE" ]; then
            tippecanoe \
              -o dist/centerline-widths/centerline_widths.pmtiles \
              --force \
              --layer "$LAYER_NAME" \
              --minimum-zoom 12 \
              --maximum-zoom 17 \
              --base-zoom 14 \
              --drop-densest-as-needed \
              "$INPUT_FILE"
          else
            echo "No WGS84 output generated; skipping PMTiles: $INPUT_FILE"
          fi

      - name: Upload PMTiles to Easyname
        if: ${{ inputs.upload_pmtiles }}
        env:
          EASYNAME_FTP_HOST: ${{ secrets.EASYNAME_FTP_HOST }}
          EASYNAME_FTP_USER: ${{ secrets.EASYNAME_FTP_USER }}
          EASYNAME_FTP_PASSWORD: ${{ secrets.EASYNAME_FTP_PASSWORD }}
        run: |
          test -f dist/centerline-widths/centerline_widths.pmtiles
          chmod +x .github/scripts/lftp-upload.sh
          .github/scripts/lftp-upload.sh dist/centerline-widths/centerline_widths.pmtiles Gehsteigbreiten/centerline_widths.pmtiles
          .github/scripts/lftp-upload.sh dist/centerline-widths/centerline_widths_summary.json Gehsteigbreiten/centerline_widths_summary.json

      - name: Upload outputs
        uses: actions/upload-artifact@v4
        with:
          name: centerline-widths
          path: |
            dist/centerline-widths/
            work/centerline-widths/*.geojsonseq
          if-no-files-found: error
